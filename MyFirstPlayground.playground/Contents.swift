//: Playground - noun: a place where people can play
//创建playground必须引入的库
import UIKit
//当一行使用多条语句的时候，需要用分号间隔开
let maxNum = 1000;print("maxNum",maxNum)
var str = "Hello, playground"
//在使用Swift关键字的时候，需要在前后加上重音符
let `class` = "Runoob"
//在语句书写上推荐使用空格来间隔运算符和变量
let a = 1 + 2

print(str)
for x in 0...10{
    //如果需要不换行，在末尾规定输出语句的最后一个字符为空字符串
    print("\(x)",terminator:" ")
}
print()

